pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'I  to Reinvented myself'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
                sh 'python3 pipeline.py'
            }
        }
    }
}